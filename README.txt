Go to the directory `obj`, open the terminal here and execute the command `make`
* Ensure that the makefile program is installed in your computer
------------------------------------------------------------------------------------------------------------------------
Allez dans le dossier `obj`, ouvrir le  terminal à ce niveau et exécuter la commande `make`
* Rassurez vous que le programme makefile soit installé dans votre machine
------------------------------------------------------------------------------------------------------------------------